CREATE TABLE USERS(
ID INTEGER,
FULL_NAME VARCHAR NOT NULL,
EMAIL VARCHAR UNIQUE NOT NULL,
PASSWORD_HASH VARCHAR NOT NULL,
PASSWORD_BIOMETRY IMAGE NULL,
PERMISSION_LEVEL INTEGER NOT NULL,

PRIMARY KEY (ID AUTOINCREMENT));
